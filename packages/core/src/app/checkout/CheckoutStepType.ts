enum CheckoutStepType {
    Billing = 'billing',
    Customer = 'customer',
    Payment = 'payment',
    Shipping = 'shipping',
    QualifyingCredentials = 'qualifying-credentials',
}

export default CheckoutStepType;
