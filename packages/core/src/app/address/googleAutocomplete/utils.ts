// These utilities belong to the Places API (New) implementation.
// They live in placesNew/utils.ts — this file re-exports them for backwards compatibility.
export {
    mapToAutocompleteItems,
    mapToGeocoderAddressComponent,
    mapToIncludedPrimaryTypes,
} from './placesNew/utils';
