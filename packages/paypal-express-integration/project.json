{
  "name": "paypal-express-integration",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/paypal-express-integration/src",
  "projectType": "library",
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint --no-error-on-unmatched-pattern \"packages/paypal-express-integration/src/**/*.ts\" \"packages/paypal-express-integration/src/**/*.tsx\""
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": [
        "{workspaceRoot}/coverage/packages/paypal-express-integration"
      ],
      "options": {
        "jestConfig": "packages/paypal-express-integration/jest.config.js"
      }
    }
  },
  "tags": [
    "scope:integration"
  ]
}