{
  "$schema": "http://json-schema.org/schema",
  "$id": "PolyfillGenerator",
  "title": "Polyfill Generator",
  "type": "object",
  "properties": {
    "projectName": {
      "type": "string",
      "description": "The name of the project where the polyfill file should be generated."
    },
    "browserslistQuery": {
      "type": "string",
      "description": "The browserslist query to determine which polyfills are needed.",
      "default": "defaults"
    },
    "outputPath": {
      "type": "string",
      "description": "The path where the polyfill file should be generated.",
      "default": "src/app/generated/polyfill/index.ts"
    }
  },
  "required": [
    "projectName"
  ]
}
